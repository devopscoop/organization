# Brewfile for organization
#
# Installs every CLI tool used or referenced by this repo.
# Usage: brew bundle

# git - the bootstrap.md multi-remote workflow (Codeberg/GitLab/GitHub) is the
# only local tooling these docs require
brew "git"
