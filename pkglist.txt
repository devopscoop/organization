# Arch Linux package list for organization — the pacman/AUR equivalent of the
# Brewfile. Installs every CLI tool used or referenced by this repo.
#
# All packages are in the official repos, so plain pacman works (as do yay/paru):
#
# Usage:
#   grep -vE '^(#|$)' pkglist.txt | sudo pacman -S --needed -

# git — the bootstrap.md multi-remote workflow (Codeberg/GitLab/GitHub) is the
# only local tooling these docs require
git
