# Arch Linux package list for organization — the pacman/AUR equivalent of the
# Brewfile. Installs every CLI tool used or referenced by this repo.
#
# claude-code is AUR-only; the rest are in the official repos. So either use an
# AUR helper for everything:
#
#   grep -vE '^(#|$)' pkglist.txt | yay -S --needed -
#
# or plain pacman for the official packages, and claude-code separately:
#
#   grep -vE '^(#|$|claude-code)' pkglist.txt | sudo pacman -S --needed -

# git — the bootstrap.md multi-remote workflow (Codeberg/GitLab/GitHub)
git

# github-cli (gh) — claude_github_actions.md distributes the
# CLAUDE_CODE_OAUTH_TOKEN secret
github-cli

# claude-code (AUR) — claude_github_actions.md creates the token with
# `claude setup-token`
claude-code
